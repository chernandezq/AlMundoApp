import React from 'react';
import {StyleSheet, Text} from 'react-native';

const Hotels: () => React = () => {
  return <Text>Hola AlMundo</Text>;
};

const styles = StyleSheet.create({});

export default Hotels;
